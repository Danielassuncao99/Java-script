// TIPOS DE DADOS PRIMITIVOS ->string, number, undefined, null ,boolean.

const nome = 'daniel'; // string
const nome1 = 'dandan';// string
const nome2 = 'assuncao';// string
const num1 = 10; // number
const num2 = 10.52 //number
let nomeAluno; // undefined -> não aponta para local nenhuma da memória
const sobrenomeAluno = null; // nulo -> não aponta para local nenhuma da memoria 
const aprovado = false; // boolean = true , false (lógico)

console.log(typeof nome);